/**
 * Side-effect module sob stub standalone.
 *
 * No app em nuvem, 'firebase/compat/auth' e 'firebase/compat/database'
 * registram os serviços na instância compat. Sob o stub, os serviços já são
 * fornecidos diretamente por firebaseStandalone.ts — este módulo existe só
 * pra que os imports de side-effect resolvam sem tocar rede ou SDK.
 */
export {};
